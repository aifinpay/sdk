{
  "$schema": "https://modelcontextprotocol.io/schemas/server.json",
  "name": "aifinpay",
  "version": "0.1.0-alpha.2",
  "description": "Stripe for AI agents. Pay for any HTTP API in stablecoins or native tokens on Polygon and Solana mainnet with one MCP call. Non-custodial, on-chain settlement.",
  "vendor": "CoinSecurities (SECCO)",
  "license": "MIT",
  "homepage": "https://aifinpay.company",
  "repository": "https://github.com/AiFinPay/sdk",
  "icon": "https://aifinpay.company/favicon.png",
  "categories": ["payments", "web3", "agent-tools"],
  "tags": [
    "payments", "ai-agents", "x402", "polygon", "solana",
    "stablecoins", "usdc", "usdt", "stripe-for-ai", "non-custodial"
  ],
  "install": {
    "command": "npx",
    "args": ["@aifinpay/mcp"]
  },
  "claude_desktop_config": {
    "mcpServers": {
      "aifinpay": {
        "command": "npx",
        "args": ["@aifinpay/mcp"]
      }
    }
  },
  "env": {
    "AIFINPAY_AGENT_SECRET": "base58 Ed25519 secret (persistent agent identity)",
    "AIFINPAY_MAX_USD": "hard cap per call, default 0.10",
    "AIFINPAY_CHAIN": "polygon | solana | auto, default auto"
  },
  "tools": [
    {
      "name": "payable_fetch",
      "description": "Fetch any URL; auto-settle 402 on-chain and retry. Returns the gated response body."
    },
    {
      "name": "agent_address",
      "description": "Return the agent's wallet address (for funding)."
    },
    {
      "name": "agent_quote",
      "description": "Get a price quote for a 402-gated endpoint without paying."
    },
    {
      "name": "pay_with_split",
      "description": "Execute a payment through the splitter contract for B2B endpoints (atomic 99/1 split)."
    },
    {
      "name": "quote_split",
      "description": "Pure-view fee breakdown for an atomic split payment."
    }
  ],
  "live_evidence": [
    { "chain": "polygon", "use_case": "Exa search", "tx": "0xeb13c5ed59c8700" },
    { "chain": "polygon", "use_case": "io.net Llama-3.3-70B, $0.025", "tx": "0x7c6ca0ff129f0a" }
  ]
}
